@inject NavigationManager _navManager

@code {
    [Parameter]
    public string RedirectURL { get; set; }

    protected override void OnInitialized()
    {
        _navManager.NavigateTo(RedirectURL ?? "");
    }
}